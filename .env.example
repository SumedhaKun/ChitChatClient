# Local dev (default when running npm run dev)
NEXT_PUBLIC_MESSAGE_SERVER_URL=ws://localhost:8080

# Production (optional override — auto-detected on Vercel if unset)
# NEXT_PUBLIC_MESSAGE_SERVER_URL=wss://chit-chat-client-zeta.vercel.app/api/server
